/**
 * Radios base para inputs, botones y tarjetas.
 * Kivo busca esquinas suaves para sentirse amigable y moderno.
 */
export const radius = {
    sm: 10,
    md: 12,
    lg: 16,
    xl: 20,
    "2xl": 24,
    full: 999,
} as const;